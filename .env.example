# Copy to .env and fill in a valid, Realtime-capable OpenAI API key.
# Realtime API is billed (~$32 / $64 per 1M audio input / output tokens).
# .env is gitignored — never commit a real key.
OPENAI_API_KEY=sk-proj-...
